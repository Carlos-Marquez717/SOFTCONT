[phases.setup]
nixPkgs = [
  "python311",  # o python310 si usas otra versión
  "glib", 
  "gobject-introspection",
  "cairo",
  "pango",
  "gdk-pixbuf",
  "libffi"
]

[phases.install]
cmds = ["pip install -r requirements.txt"]
